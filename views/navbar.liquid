{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <a href="/" class="navbar-brand">Gainz</a>
    {% if loggedIn %}
    <a href="/client/bio" class="nav-link text-light">Hello {{ username }} </a>
    {% endif %}
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      {% if loggedIn %}

      <!-- <a href="/food/new" class="nav-link text-light">Add Food</a>
      <a href="/foodDiary/new" class="nav-link text-light">Add Day</a> -->
      <a href="client/myMacros" class="nav-link text-light">My Macros</a>
      <a href="/food/mine" class="nav-link text-light">View My Foods</a>
      <a href="/foodDiary/mine" class="nav-link text-light">My Diary Entries</a>
      <a href="/auth/logout" class="nav-link text-light">Log Out</a>
      {% else %}
      <a href="/auth/signup" class="nav-link text-light">Sign Up</a>
      <a href="/auth/login" class="nav-link text-light">Sign In</a>
      {% endif %}
    </div>
  </div>
</nav>
{% endblock %}
